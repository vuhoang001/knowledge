# Cấu hình Obsidian — repo này đọc/ghi chính bằng VS Code. Giữ thư mục lại để lỡ
# muốn mở bằng Obsidian thì vẫn chạy, nhưng không commit: bố cục pane đổi liên tục,
# commit vào chỉ tạo nhiễu diff.
.obsidian/
.trash/

# Rác hệ điều hành
.DS_Store
Thumbs.db

# Lab dbt/Flink nếu lỡ tạo trong repo — lab sống ở ~/Documents/learn-lab/
*.duckdb
target/
dbt_packages/
logs/

# Docusaurus
/node_modules
/build
/.docusaurus
/.cache-loader
npm-debug.log*
