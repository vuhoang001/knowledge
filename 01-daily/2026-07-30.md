---
date: 2026-07-30
tags: [daily]
---

# 30/07/2026

## Làm gì

- Rà toàn bộ 47 bảng trong `hdos-v3`, thêm phân trang + lọc cho 17 bảng còn thiếu.
  Dựng primitive dùng chung `shared/ui/data-table.tsx` + `data-table-filter.ts`
  thay vì chép logic 19 lần.
- Deploy lên `.60` — xem [[deploy-fe-len-60]].
- Dựng vault này.

## Vướng gì / sửa ra sao

- **Không phải bảng nào cũng nên phân trang.** Đợt rà đầu định làm hết cho "đủ",
  nhưng bảng tổng hợp 4–8 dòng cố định (KPI EMS, benchmark liên viện, 8 registry)
  mà gắn pager vào thì chỉ là nhiễu. Chốt: chỉ làm cho bảng **danh sách không
  giới hạn số dòng**.
- **Bảng đã phân trang phía server thì TUYỆT ĐỐI không bọc lọc client vào.**
  M07 kho thuốc, M10 chất lượng, các drawer drill-down đều `total` từ backend —
  lọc client chỉ lọc được đúng trang đang cầm, ra kết quả SAI mà nhìn vẫn hợp lý.
  Đây là cái bẫy dễ dính nhất.
- Lỗi tự gây: đặt `const FILTERS` dùng `CAN_DATE_NGAY` **phía trên** chỗ khai báo
  hằng đó → TDZ, chết lúc load module. `tsc` không bắt. Nhớ: const ở top-level
  module được đánh giá NGAY, không hoist giá trị như `function`.
- Lint `react-refresh/only-export-components` kêu vì `data-table.tsx` export cả
  hằng lẫn component. Repo đã có tiền lệ tách (`department-filter.ts` vs
  `department-filter-controls.tsx`) → dồn hằng + logic sang file `.ts`, file
  `.tsx` chỉ còn component.

## Đáng nhấc lên `20-notes/`

- Client-side vs server-side pagination: khi nào chọn cái nào, và dấu hiệu nhận
  biết một bảng đang phân trang phía server (`total` đến từ response, không phải
  `dataSource.length`).
- Thói quen `md5` đối chiếu cây thư mục trước khi deploy đè — đã cứu một lần hôm nay.
